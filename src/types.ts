export interface NotesType {
    year: string;
    sem: string;
    subjects: Array<string>;
}

export interface userType {
    name: string;
    year: string;
    sem: string;
    uid: string;
    providerId: string;
}
