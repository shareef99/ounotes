export interface NotesType {
    year: string;
    sem: string;
    subject: string;
    createdAt: string;
    createdBy: string;
    email: string;
    url: string;
}

export interface userType {
    name: string;
    year: string;
    sem: string;
    uid: string;
    providerId: string;
    email: string;
}
